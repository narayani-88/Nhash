{
  "name": "hashsleuth",
  "version": "1.0.0",
  "description": "HashSleuth - Identify cryptographic hashes",
  "scripts": {
    "build": "npm install",
    "dev": "live-server public --port=8888"
  },
  "dependencies": {
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
